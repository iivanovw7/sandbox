/**
 * Module contains `ProfileUnlock` constants.
 * @module src/entities/ProfileLock/constants
 */

/**
 * Pin Code length.
 * @type {number}
 */
export const PIN_LENGTH = 4;

/**
 * Default Pin Code.
 * @type {number}
 */
export const DEFAULT_PIN = new Array(PIN_LENGTH).fill('');
